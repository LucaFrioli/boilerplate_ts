node_modules
.env
.env.*
logs
dist
*.tsbuildinfo
# Templates públicos de env — são documentação e DEVEM ser commitados
!.env.example
!.env.test.example
# Pasta gerada pelo `npm run test:coverage` — não commitar relatórios de cobertura
coverage
/docs/Auditoria
